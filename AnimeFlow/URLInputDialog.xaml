<Window x:Class="AnimeFlow.URLInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        Title="Open URL" 
        Height="200" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        Background="#FF1E1E1E"
        ResizeMode="NoResize"
        ui:WindowHelper.UseModernWindowStyle="True">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Enter video URL:" 
                   Foreground="White" 
                   FontSize="14"
                   Margin="0,0,0,10"/>

        <TextBox x:Name="UrlTextBox" 
                 Grid.Row="1"
                 Height="30"
                 FontSize="13"
                 Padding="8,5"
                 KeyDown="UrlTextBox_KeyDown"/>

        <TextBlock Grid.Row="2" 
                   Text="Supported sites: YouTube, Twitch, Crunchyroll, and 1000+ more" 
                   Foreground="#FFAAAAAA" 
                   FontSize="11"
                   Margin="0,8,0,0"/>

        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Cancel" 
                    Width="80" 
                    Height="32"
                    Margin="0,0,10,0"
                    Click="Cancel_Click"/>
            <Button Content="Open" 
                    Width="80" 
                    Height="32"
                    Click="Open_Click"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
